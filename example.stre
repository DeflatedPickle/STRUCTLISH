EXAMPLE

IF INTEGER 3 IDENTICAL TO INTEGER 3 THEN
	PRINT STRING This number is identical to this other variable.
ENDIF

IF INTEGER 5 MORE THAN INTEGER 3 THEN
	PRINT STRING This number is more than this other number.
ENDIF

IF INTEGER 3 LESS THAN INTEGER 5 THEN
	PRINT STRING This number is less than this other number.
ENDIF

EXIT